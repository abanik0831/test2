<% if @profile_pic.new_record? %>
alert("Failed to upload painting: <%= j @profile_pic.errors.full_messages.join(', ').html_safe %>");
<% else %>
$("#paintings").append("<%= j render(@profile_pic) %>");
<% end %>