<h1>Listing deleteposts</h1>

<table>
  <thead>
    <tr>
      <th>Post</th>
      <th>User</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @deleteposts.each do |deletepost| %>
      <tr>
        <td><%= deletepost.post_id %></td>
        <td><%= deletepost.user_id %></td>
        <td><%= link_to 'Show', deletepost %></td>
        <td><%= link_to 'Edit', edit_deletepost_path(deletepost) %></td>
        <td><%= link_to 'Destroy', deletepost, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Deletepost', new_deletepost_path %>
