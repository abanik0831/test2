<%= form_for(@deletepost) do |f| %>
  <% if @deletepost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@deletepost.errors.count, "error") %> prohibited this deletepost from being saved:</h2>

      <ul>
      <% @deletepost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
