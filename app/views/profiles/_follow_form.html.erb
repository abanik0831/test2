<% if current_user.id != @profiles.id %>
    <div id="test"></div>
    <div id="follow_form">
      <% if current_user.following?(@profile) %>
        <%= render 'unfollow' %>
      <% else %>
        <%= render 'follow' %>
      <% end %>
    </div>
<% end %>