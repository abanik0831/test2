<%= form_for(current_user.relationships.find_by(followed_id: @profile),
             html: { method: :delete }, remote: true) do |f| %>
    <%= f.submit "Unfollow", :class => "follow_user btn btn-large" %>
<% end %>

